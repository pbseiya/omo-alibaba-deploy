{
  "mcpServers": {
    "tavily": {
      "command": "npx",
      "args": [
        "-y",
        "@tavily/mcp@latest"
      ],
      "env": {
        "TAVILY_API_KEY": "{env:TAVILY_API_KEY}"
      }
    }
  }
}